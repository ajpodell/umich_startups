<%provide(:title, @user.name) %>
<h1><%= @user.name %></h1>
<%= image_tag @user.photo.url(:thumb) %>
email: <%= @user.email %><br/>
	
	<% if false %>
		<%= gravatar_for @user, size: 100 %><br/>
	<% end %>