<%= form_for(current_user.connections.find_by(followed_id: @user),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Disconnect", class: "btn btn-large" %>
<% end %>