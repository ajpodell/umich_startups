<% provide(:title, @title) %>
<div class="row">
  

  <!-- <aside class="span4">
    <section>
      <%= link_to image_tag(@user.photo.url(:medium)), @user %>
      <h1><%= @user.name %></h1>
      <span><%= link_to "view my profile", @user %></span>
    </section>
    <section>

      <% if false %>
       <%= render 'shared/stats' %>
      <% end %>

      <% if @users.any? %>
        <div class="user_avatars">
          <% @users.each do |user| %>
            <%= link_to image_tag(user.photo.url(:tiny)), user %>
          <% end %>
        </div>
      <% end %>
    </section>
  </aside> -->


  <div class="span8">
    <% if @users.any? %>
      <h3><%= @title %></h3>
      <ul class="users">
        <%= render @users %>
      </ul>
      <%= will_paginate %>
    <% else %>
      <h3>No connections to display</h3>
    <% end %>
  </div>
</div>